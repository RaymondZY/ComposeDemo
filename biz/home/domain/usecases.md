# Home 模块功能需求

## 功能点

1. **初始状态**
    - 默认选中首页（HOME）标签页。

2. **切换标签页**
    - 点击不同标签页时，选中状态更新为对应标签页。
    - 重复选择同一标签页不触发状态变更。

3. **中间按钮**
    - 点击底部导航栏中间按钮不改变当前选中的标签页。

4. **角标更新**
    - 更新角标后，对应标签页显示角标数据。
    - 不同标签页的角标数据互不覆盖。
